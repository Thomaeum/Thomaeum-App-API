/**
 * This Router handles all Requests send to /auth
 * @type {Router}
 */

const router = require('express').Router();
const logger = log4js.getLogger('catho')

// Requests here

module.exports = router;

logger.info('Loaded Catho-Route')